/*
package org.skypro.skyshop.product;

import org.skypro.skyshop.searchProduct.Searchable;

public class eduk {

  public static void main(String[] args) {
    searchForMostSuitable("abc");

  }
    public static void searchForMostSuitable (String substring){
      String str = "abcvvvabcvvvabcjhbjybuyuygabc";
      for (Searchable object : searchList)
      int count = 0;
      int idx = 0;
      int indexStr = str.indexOf(substring, idx);

      while (indexStr != -1) {
        count++;
        idx = indexStr + substring.length();
        indexStr = str.indexOf(substring, idx);
      }
      System.out.println("Количество повторений: " + count);
    }



*/






    /* public static void main(String[] args) {
    String str = "abcabcvbabcbvvabcjjjjjabc";
    String substring = "abc";
    int count = countOccurrences(str, substring);
    System.out.println("Количество повторений: " + count);
  }
  public static int countOccurrences(String str, String substring) {
    int count = 0;
    for (int idx = 0; (idx = str.indexOf(substring, idx)) != -1; idx += substring.length()) {
      count++;
    }
    return count;
  }*/





//}//
